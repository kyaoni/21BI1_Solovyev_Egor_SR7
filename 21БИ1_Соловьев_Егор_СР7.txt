нач
	try:
		вывод('Введите целое десятичное число')
		ввод(n)
	except ValueError:
		вывести('Ошибка ввода')
		выход из программы
	else:
		функция function:
			x=abs(n)
			c=0
			пока x>0:	
				x//=10
				c+=1
			last=abs(n)%10
			если last%2!=0:
				вывод('Ошибка, последняя цифра нечётная')
			иначе:
				p=10**(c-1)
				first=abs(n)//p
				если first%2!=0:
					вывод('Ошибка, первая цифра нечётная'))
				иначе:
					m=abs(n)-(first*p)-last
					f=(last*p)+first+m
					если n>0:
						вывод(f)
					иначе:
						вывод(-f)
	выполнить function
кон